[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
markers =
    integration: optional live checks against Docker/running services
filterwarnings =
    ignore::DeprecationWarning
